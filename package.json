{
  "name": "release-radar",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --port 8787 --test-scheduled --remote",
    "start": "wrangler dev --port 8787 --test-scheduled --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@octokit/request": "^8.1.4",
    "@octokit/types": "^12.1.1",
    "@types/semver": "^7.5.4",
    "discord-api-types": "^0.37.61",
    "semver": "^7.5.4"
  }
}
